import { RetrivenamePipe } from './retrivename.pipe';

describe('RetrivenamePipe', () => {
  it('create an instance', () => {
    const pipe = new RetrivenamePipe();
    expect(pipe).toBeTruthy();
  });
});
